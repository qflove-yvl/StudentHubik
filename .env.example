# Core
SECRET_KEY=replace-with-long-random-string-at-least-32-chars
DATABASE_URL=sqlite:///site.db
ENVIRONMENT=development
REQUIRE_STRONG_SECRET_IN_PROD=1

# Admin bootstrap
ADMIN_EMAIL=admin@studenthubik.local
ADMIN_PASSWORD=change-me-now

# Login throttling
LOGIN_MAX_ATTEMPTS=5
LOGIN_BLOCK_MINUTES=10

# Password reset
PASSWORD_RESET_TOKEN_MINUTES=30
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM_EMAIL=admin@studenthubik.local
SMTP_USE_TLS=1
SMTP_USE_SSL=0
SMTP_TIMEOUT_SECONDS=15

# Optional error monitoring
SENTRY_DSN=

PASSWORD_RESET_MIN_INTERVAL_SECONDS=60
DEBUG_SHOW_RESET_LINK_ON_EMAIL_FAIL=1

MAX_SCHEDULE_HISTORY=20

COLLEGE_NAME=Красноярский колледж отраслевых технологий и предпринимательства
